# Current D1 Filter V2 Validation

Strategy: `MICRO_DAILY_FILTER_CURRENT_D1_V2`
Canonical profile: `IC_MARKETS_NY_CLOSE_FOREX_V1`
Reference range: 2026-07-05 00:00 UTC through 2026-08-05 08:00 UTC

## Structural result

PASS. Against the sanitized IC Markets Global MT5 export, canonical timestamp
membership matched exactly:

| Timeframe | IC Markets | Spect8 | Shared | Reference-only | Spect8-only |
|---|---:|---:|---:|---:|---:|
| H1 | 539 | 539 | 539 | 0 | 0 |
| H4 | 134 | 134 | 134 | 0 | 0 |
| D1 | 22 | 22 | 22 | 0 | 0 |

Canonical weekend H1 count was zero. H4 and D1 were reconstructed from the
same canonical H1 stream; provider-native H4/D1 were not strategy inputs.

## Manually inspectable V2 calculations

At `2026-07-06T09:00:00Z`, 12 completed current-session H1 bars produced
OHLC `1.14424 / 1.14440 / 1.14153 / 1.14201`. Direct aggregation of those
same H1 bars produced identical values. Previous D1 closed Friday
`2026-07-03T21:00:00Z`; Wilder ATR(5) was `0.0062756352`, buffer was
`0.000313781760`, BUY was `1.14153 <= 1.142403781760` (true), and SELL was
`1.14440 >= 1.145896218240` (false). Classification: `BUY`. This H1 close
also completed H4, so both evaluations use snapshot
`dfs_d310c9f2b786112f903619c14ca7cb9e761fb28c0c8a2abd499b1a9812f531c9`.

At `2026-07-15T09:00:00Z`, 12 H1 bars produced
`1.14212 / 1.14437 / 1.14177 / 1.14243`, exactly matching direct aggregation.
ATR(5) was `0.0058219264`; neither side matched. This was also an H4 close.

At `2026-08-05T08:00:00Z`, 11 H1 bars produced
`1.15331 / 1.15462 / 1.15262 / 1.15393`, exactly matching direct aggregation.
ATR(5) was `0.0062243008`, buffer `0.000311215040`, BUY was false, SELL was
`1.15462 >= 1.153128784960` (true), classification `SELL`.

## Deterministic replay

Two independent dry runs over 2026-07-05 through `2026-08-05T12:00:00Z`
each produced:

- 543 canonical H1 inputs and shared snapshots
- 135 H4 evaluations
- 22 completed D1 sessions in the display range
- 10 expected market closures and zero unexpected gaps
- source checksum `3aa65b2bcba882c2e47c559bd2264a271ee2f796c86306b7b76412b4bd10a9f7`
- output checksum `c4a4b47b7bd6302c621c19ec988fa034dc0c28b2931623b336a2ccf48dcc4fbc`
- zero quarantined inputs after bounding aggregation to observable source data

The run ID derived from deterministic output was
`current-d1-v2-c4a4b47b7bd6302c`. Runtime start/finish timestamps are manifest
metadata and are intentionally excluded from the output checksum.

The guarded development apply inserted 542 snapshots and 677 versioned
evaluations. A second apply inserted zero snapshots/evaluations, recognized all
677 evaluations as duplicates, and reproduced the same output checksum. The
guarded apply created a local pre-apply backup under ignored `var/` storage; no
database or backup is a repository artifact.

## Current persisted acceptance calculation

At `2026-08-05T12:00:00Z`, the persisted snapshot was reconstructed from 15
completed H1 bars. The current partial D1 high/low were `1.15556` and
`1.15262`; the previous completed D1 high/low were `1.15344` and `1.15030`.
Wilder ATR(5) was `0.0062243008`, so the buffer was `0.000311215040`.
BUY was `1.15262 <= 1.150611215040` (false); SELL was
`1.15556 >= 1.153128784960` (true). Classification: `SELL`.

The client-supplied approximate example listed previous D1 low `1.15300` but
also listed BUY threshold `1.150311215040`; those values cannot produce one
another. Persisted canonical candles and exact backend decimal values are the
acceptance authority.

Machine-readable structural evidence is generated by:

```powershell
.\.venv\Scripts\python.exe -m scripts.validate_current_d1_filter_v2 `
  --database var/spect8_phase1.sqlite3 `
  --reference-dir backend/data/icmarkets_reference `
  --output backend/data/icmarkets_reference/current_d1_filter_v2_validation.json
```
